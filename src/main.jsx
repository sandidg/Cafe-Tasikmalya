import React from "react"
import ReactDOM from "react-dom/client"
import CafeApp from "./CafeApp.jsx"
import "./index.css"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode><CafeApp /></React.StrictMode>
)
